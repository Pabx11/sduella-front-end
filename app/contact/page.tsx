import { Contact } from '../../src/components/RouteViews';
import { pageMetadata } from '../../src/lib/site';
export const metadata = pageMetadata({ title: 'Contact Sduella', description: 'Contact Sduella about opportunity sources, corrections, partnerships and platform support.', path: '/contact' });
export default function Page() { return <Contact />; }
