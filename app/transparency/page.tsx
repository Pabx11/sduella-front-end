import { Transparency } from '../../src/components/RouteViews';
import { pageMetadata } from '../../src/lib/site';
export const metadata = pageMetadata({ title: 'Source & Listing Transparency', description: 'Understand how Sduella collects, verifies, normalises and links opportunity information to original providers.', path: '/transparency' });
export default function Page() { return <Transparency />; }
